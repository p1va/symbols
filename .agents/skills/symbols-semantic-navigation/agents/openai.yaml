interface:
  display_name: "Symbols Semantic Navigation"
  short_description: "Use LSP-backed navigation and rename"
  default_prompt: "Use $symbols-semantic-navigation to inspect this codebase semantically and prefer Symbols tools over grep."
